export const AppConfig = {
    locales: ['en', 'es', 'fr'], // Supported locales (e.g., English, Spanish, French)
    localePrefix: 'always', // Can be 'always', 'never', or 'as-needed'
    defaultLocale: 'en', // Default language for the application
  };
  